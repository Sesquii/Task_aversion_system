feat: Add cache refresh buttons for monitored metrics updates

Add refresh/update buttons to clear caches and reload data for monitored
metrics, allowing users to quickly update metrics between tasks without
waiting for automatic cache expiration.

## Summary

- Settings page: Added "Refresh Data & Clear Cache" button at top of settings
- Monitored metrics: Added "Update" button next to Edit button for quick refresh
- Cache clearing: Invalidates all relevant caches (InstanceManager, Analytics class-level and instance-level)
- Data reload: Forces fresh data reload for accurate metrics display

## Details

### Settings Page (ui/settings_page.py)
- Added refresh button at top of settings page (above all other sections)
- Clears InstanceManager, TaskManager, and Analytics caches (class-level and instance-level)
- Clears dashboard's Analytics instance cache if available
- Reloads user state, instance manager, and task manager data
- Full page reload to display fresh data

### Dashboard Monitored Metrics (ui/dashboard.py)
- Added `update_monitored_metrics()` function for quick cache clearing
- Added "Update" button next to Edit button in monitored metrics header
- Green button styling to distinguish from Edit button
- Focused cache clearing for metrics-related data (InstanceManager, Analytics)
- Page reload to refresh metrics display

## Benefits

- Quick updates: Users can manually refresh metrics immediately after completing tasks
- No waiting: Bypasses cache TTL (5 minutes) for instant updates
- Convenient placement: Update button right next to monitored metrics for easy access
- Comprehensive: Settings button clears all caches for full system refresh
