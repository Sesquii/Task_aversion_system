# Production Environment Variables
# Copy to task_aversion_app/.env.production and fill in real values.
# Never commit .env.production. See docs/server_migration_checklist.md Phase 3.1.

# === REQUIRED ===
# PostgreSQL connection (production MUST use PostgreSQL)
DATABASE_URL=postgresql://task_aversion_user:YOUR_PASSWORD@localhost:5432/task_aversion_system

# NiceGUI bind address (0.0.0.0 to accept connections from nginx)
NICEGUI_HOST=0.0.0.0
NICEGUI_PORT=8080

# Session storage secret (generate a strong random string for production)
STORAGE_SECRET=generate-a-strong-random-string-here

# Environment identifier
ENVIRONMENT=production

# === OAUTH (Required for Google login) ===
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret
OAUTH_REDIRECT_URI=https://TaskAversionSystem.com/auth/callback

# Optional: session expiry in days (default 30)
# SESSION_EXPIRY_DAYS=30

# === OPTIONAL ===
# Disable CSV fallback in production (recommended: true)
# DISABLE_CSV_FALLBACK=true

# Query logging (set to 0 to reduce log volume in production)
# ENABLE_QUERY_LOGGING=0

# Logging to file
# LOG_TO_FILE=true
# APP_LOG_FILE=/var/log/task-aversion-app/app.log
# LOG_TEE=false

# Cache busting for assets
# ENABLE_CACHE_BUSTING=1
